# Pulled Documentation Archive

This directory contains documentation copied from another branch for review.
It is source/reference material, not the canonical target for the current
implementation.

Canonical docs for the current branch are:

- `docs/schema_lifecycle_refactor_plan.md`
- `docs/schema_lifecycle_refactor_progress.md`
- `docs/entity_deletion_policy.md`
- `docs/database_erd.md` after the schema refactor modules update it

Do not update the pulled files as the implementation target. Port relevant
decisions into canonical docs under `docs/`, then update implementation and
contracts from those canonical docs.

