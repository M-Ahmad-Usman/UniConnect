
// This will be populated as needed. Common uses:
// - vi.clearAllMocks() to reset mock state between files
// - Setting up fake timers globally
// - Configuring environment variables for tests

// For now, Ensure NODE_ENV is 'test' so any environment-sensitive
// code in the app behaves correctly during tests.
process.env.NODE_ENV = 'test'