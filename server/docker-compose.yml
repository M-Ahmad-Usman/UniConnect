services:
  dev-db:
    image: postgres:18.3-bookworm
    container_name: uniconnect-dev-pg
    ports:
      - 5433:5432
    env_file:
      - .env
    volumes:
      - uniconnect-dev-pgData:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  uniconnect-dev-pgData: