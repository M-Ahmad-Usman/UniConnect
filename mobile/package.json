{
  "name": "uniconnect-mobile",
  "version": "1.0.0",
  "description": "UniConnect Android wrapper (Capacitor)",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:android": "cap sync android && cap build android",
    "generate:assets": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  },
  "devDependencies": {
    "sharp": "^0.35.1",
    "typescript": "^6.0.3"
  }
}
